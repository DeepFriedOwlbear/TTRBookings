@page
@model TTRBookings.Web.Pages.Roses.DetailsModel
@{
}
<h2>Rose Details</h2>
<table class="table table-hover">
    <thead class="thead-dark">
        <tr>
            <th>Rose</th>
            <th>Options</th>
        </tr>
    </thead>
    <tr>
        <td>@Model.Rose.Name</td>
        <td>
            <a asp-page="./Edit" asp-route-id="@Model.Rose.Id" class="btn btn-primary btn-sm">Edit</a>
            <a asp-page="./Delete" asp-route-id="@Model.Rose.Id" class="btn btn-danger btn-sm">Delete</a>
        </td>
    </tr>
</table>