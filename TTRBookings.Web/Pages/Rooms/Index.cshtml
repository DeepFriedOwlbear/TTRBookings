@page
@model IndexModel
@{
    ViewData["Title"] = "Rooms";
}

<table class="table table-hover">
    <thead class="thead-dark">
        <tr>
            <th>Rose</th>
            <th>Details Page</th>
        </tr>
    </thead>
    @foreach (var room in Model.Rooms)
    {
        <tr>
            <td>@room.Name</td>
            <td><a asp-page="./Details" asp-route-id="@room.Id">Details</a></td>
        </tr>
    }
</table>

